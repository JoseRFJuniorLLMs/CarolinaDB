-- CarolinaDB machine-checked models. Each module states a machine the project already
-- describes elsewhere and proves its safety invariants for *arbitrary* parameters, where the
-- bounded TLC runs and the Rust explicit-state checkers only enumerate a finite slice.
import Carolina.Escrow
import Carolina.Check
